{% extends 'base.html.twig' %}

{% block title %}Annonce{% endblock %}

{% block body %}
    <h1>Annonce</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_annonce</th>
                <td>{{ annonce.getIdAnnonce() }}</td>
            </tr>
            <tr>
                <th>Date_debut</th>
                <td>{{ annonce.dateDebut ? annonce.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ annonce.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_annonce_index') }}">back to list</a>

    <a href="{{ path('app_annonce_edit', {'id_annonce': annonce.getIdAnnonce()}) }}">edit</a>

    {{ include('annonce/_delete_form.html.twig') }}
{% endblock %}
