<form method="post" action="{{ path('app_location_delete', {'id_location': location.getIdLocation()}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ location.getIdLocation()) }}">
    <button class="btn">Delete</button>
</form>
