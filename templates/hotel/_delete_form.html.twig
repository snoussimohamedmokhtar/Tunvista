<form method="post" action="{{ path('app_hotel_delete', {'idH': hotel.idH}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hotel.idH) }}">
    <button class="btn">Delete</button>
</form>
