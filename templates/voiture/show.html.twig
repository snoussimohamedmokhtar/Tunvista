{% extends 'base.html.twig' %}

{% block title %}Voiture{% endblock %}

{% block body %}
    <h1>Voiture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_voiture</th>
                <td>{{ voiture.idVoiture }}</td>
            </tr>
            <tr>
                <th>Marque</th>
                <td>{{ voiture.marque }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ voiture.annee ? voiture.annee|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Prix_j</th>
                <td>{{ voiture.prixJ }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_voiture_index') }}">back to list</a>

    <a href="{{ path('app_voiture_edit', {'id_voiture': voiture.getIdVoiture()}) }}">edit</a>

    {{ include('voiture/_delete_form.html.twig') }}
{% endblock %}
